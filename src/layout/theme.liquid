<!doctype html>
<html lang="{{ shop.locale }}">
<head>
{% include 'head-tags' %}
{{ content_for_header }}
</head>
<body id="body">
  <div id="view">
    {% include "header" %}
    <div id="page">
      {{ content_for_layout }}
    </div>
    {% include "footer" %}
  </div>
  <vue-mobile-menu>
    {% include 'mobile-menu' %}
  </vue-mobile-menu>
  <vue-mini-cart></vue-mini-cart>
  {% include 'script-tags' %}
</body>
</html>