<!doctype html>
<html lang="{{ shop.locale }}">
<head>
{% include 'head-tags' %}
{{ content_for_header }}
</head>
<body id="body">
  <div id="view">
    {% include "header" %}
    <div id="page">
      {{ content_for_layout }}
    </div>
    {% include "footer" %}
  </div>
  {% include "sidemenu" %}
  <vue-cart></vue-cart>
  {% include 'script-tags' %}
</body>
</html>